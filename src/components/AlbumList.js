import { Typography, ImageList, ImageListItem } from '@material-ui/core';
import React from 'react'
import { Link } from 'react-router-dom';

function AlbumList( {photosData} ) {
    return (
        <div>
            dsd
        </div>
    )
}

export default AlbumList
